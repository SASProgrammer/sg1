# graph.R -- 000_User_Guide
# No graph to render. See numbered example directories for working charts.
# Quick start:
#   setwd("../005_mean/R"); source("build_data.R"); source("graph.R")
message("SG1 User Guide — navigate to any numbered example to run charts.")
